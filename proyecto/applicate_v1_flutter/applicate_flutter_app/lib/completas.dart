import 'package:flutter/material.dart';

class CompletasPage extends StatelessWidget {
  const CompletasPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('COMPLETAS PAGE'),
      ),
    );
  }
}
