import 'package:flutter/material.dart';


class PendientesPage extends StatelessWidget {
  const PendientesPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('PENDIENTES PAGE'),
      ),
    );
  }
}