import 'package:flutter/material.dart';


class Completas extends StatefulWidget {
  Completas({Key key}) : super(key: key);

  @override
  _CompletasState createState() => _CompletasState();
}

class _CompletasState extends State<Completas> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Completas'),
    );
  }
}